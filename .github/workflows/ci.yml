name: ci

on:
  push:
  pull_request:

jobs:
  native-release-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - name: Native validation
        run: ./scripts/validate-artifact.sh

  docker-release-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - name: Docker build
        run: docker build -t spruce-poc .
      - name: Prepare writable artifact directory
        run: mkdir -p artifacts/docker && chmod 0777 artifacts/docker
      - name: Docker preset list
        run: docker run --rm spruce-poc list
      - name: Docker WF-128 benchmark
        run: >-
          docker run --rm --user "$(id -u):$(id -g)"
          -v "$PWD/artifacts/docker:/artifacts"
          spruce-poc bench system-n1024-wf128-crom-v1
      - name: Docker functional gate
        run: >-
          docker run --rm --user "$(id -u):$(id -g)"
          -v "$PWD/artifacts/docker:/artifacts"
          spruce-poc gate
      - name: Docker historical artifact gate
        run: >-
          docker run --rm --user "$(id -u):$(id -g)"
          -v "$PWD/artifacts/docker:/artifacts"
          spruce-poc artifact-gate
